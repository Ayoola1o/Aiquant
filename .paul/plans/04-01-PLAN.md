---
phase: 04-multi-agent-consensus-and-execution
plan: 01
plan_type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - backend/adk_agent.py
  - backend/main.py
  - frontend/src/components/AIDebatePanel.tsx
autonomous: true
description: "Orchestrate Google ADK multi-agent consensus pipeline (Analyst, Debate, Risk Supervisor, Portfolio Execution) and stream live consensus events."
type: Plan
about: "Aiquant"
---

<objective>
## Goal
Deepen Google ADK multi-agent consensus orchestration (`adk_agent.py`), connecting multi-analyst signals, Bull/Bear debate synthesis, Risk Supervisor validation, and Portfolio Manager execution into a unified API pipeline with live frontend consensus visualization (`AIDebatePanel.tsx`).

## Purpose
Enables autonomous multi-agent decision-making for trading strategy generation and execution, ensuring every trade proposal is peer-reviewed by specialized AI agents (Technical, Fundamental, Sentiment, Risk Supervisor) before hitting broker execution.

## Output
- Enhanced multi-agent pipeline in `backend/adk_agent.py`
- Multi-agent consensus API endpoints in `backend/main.py`
- Real-time consensus visualization in `frontend/src/components/AIDebatePanel.tsx`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@backend/adk_agent.py
@backend/main.py
@frontend/src/components/AIDebatePanel.tsx
</context>

<acceptance_criteria>
- [ ] Implement `run_multi_agent_consensus()` in `backend/adk_agent.py` combining Analyst, Debate, and Risk Supervisor decisions
- [ ] Add `/api/agent/consensus` POST endpoint in `backend/main.py` to trigger multi-agent analysis on any ticker
- [ ] Update `AIDebatePanel.tsx` to display real-time agent debate, consensus confidence score, and Risk Supervisor verdict
- [ ] Verification test passes with valid multi-agent JSON payload response
</acceptance_criteria>

<tasks>
<task id="1" type="execute">
<name>Refactor and finalize run_multi_agent_consensus in adk_agent.py</name>
<files>backend/adk_agent.py</files>
<action>Ensure run_multi_agent_consensus executes the ParallelAnalyst, Bull/Bear Debate synthesis, and Risk Supervisor validation pipeline cleanly with fallback handling.</action>
<verify>Verify function returns structured MarketView and SupervisorDecision payloads.</verify>
</task>

<task id="2" type="execute">
<name>Expose /api/agent/consensus endpoint in main.py</name>
<files>backend/main.py</files>
<action>Add /api/agent/consensus endpoint accepting ticker, timeframe, and user context, returning the multi-agent consensus decision.</action>
<verify>Test endpoint response using python test runner.</verify>
</task>

<task id="3" type="execute">
<name>Connect AIDebatePanel.tsx to live consensus API</name>
<files>frontend/src/components/AIDebatePanel.tsx</files>
<action>Update AIDebatePanel UI to trigger live agent consensus analyses and display real-time Bull vs Bear theses and Risk Supervisor status.</action>
<verify>Verify component prop types and clean TS compilation.</verify>
</task>
</tasks>

<verification>
- [ ] Run multi-agent pipeline test script.
- [ ] Confirm AIDebatePanel component compiles cleanly.
</verification>
