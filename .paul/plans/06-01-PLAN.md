---
phase: 06-automated-e2e-testing-and-verification
plan: 01
plan_type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - backend/test_suite.py
  - backend/main.py
autonomous: true
description: "Build an automated end-to-end backend test suite for feature engine, backtest engine, risk manager, and multi-agent consensus endpoints."
type: Plan
about: "Aiquant"
---

<objective>
## Goal
Create an automated end-to-end integration test suite (`backend/test_suite.py`) covering all core Aiquant services: FeatureEngine calculations, BacktestEngine execution, RiskManager circuit breakers, multi-agent consensus API, and best-execution order router.

## Purpose
Establishes regression testing and automated verification gates for production deployment hardening under Milestone 3, ensuring all quantitative modules run reliably without runtime crashes.

## Output
- Integration test suite `backend/test_suite.py`
- Test runner integration in `backend/main.py`
</objective>

<context>
## Project Context
@.paul/PROJECT.md
@.paul/ROADMAP.md
@.paul/STATE.md

## Source Files
@backend/backtest_engine.py
@backend/feature_engine/engine.py
@backend/trading_engine.py
@backend/adk_agent.py
@backend/main.py
</context>

<acceptance_criteria>
- [ ] Create `backend/test_suite.py` with test cases for FeatureEngine, BacktestEngine, RiskManager, and ADK consensus pipeline
- [ ] Add `/api/test/run` GET endpoint in `backend/main.py` returning automated test suite execution results
- [ ] Ensure 100% test pass rate across all test suites
</acceptance_criteria>

<tasks>
<task id="1" type="execute">
<name>Create backend/test_suite.py integration test suite</name>
<files>backend/test_suite.py</files>
<action>Implement automated unittest / pytest integration test suite verifying backtesting accuracy, feature calculation, risk circuit breakers, and ADK agent consensus outputs.</action>
<verify>Execute test_suite.py with venv python to confirm all test cases pass.</verify>
</task>

<task id="2" type="execute">
<name>Expose /api/test/run endpoint in main.py</name>
<files>backend/main.py</files>
<action>Add GET /api/test/run endpoint to execute the integration test suite and return JSON test results.</action>
<verify>Verify API response structure using python client.</verify>
</task>
</tasks>

<verification>
- [ ] Run `python backend/test_suite.py` to confirm clean test execution.
- [ ] Verify `/api/test/run` returns 100% passed test results.
</verification>
